[package]
name = "assembler"
version = "0.1.0"
edition = "2024"

[dependencies]
assembler-schema = { workspace = true }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"

[lints]
workspace = true
